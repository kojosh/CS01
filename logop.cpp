#include <iostream>
#include <sstream>
#include <string>
using namespace std;

int main()
{
    int num, x;

    //  number should be between 1 and 100
    cout << "Enter your number: ";
    cin >> num;

    // if (( num > 0 ) || ( num > 100 )) {
    //     cout << "Invalid input \n";
    //     exit(0);
    // }

    x = ! num > 10;

    if ((num >= 0) || (num <= 100)) {

        
        
        
        cout << " do something \n";
    }
}